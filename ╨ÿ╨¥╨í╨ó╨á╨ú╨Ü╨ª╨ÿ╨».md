# 📱 Инструкция: Как загрузить проект в GitHub и собрать APK

## Вариант 1: Автоматическая сборка в GitHub (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте репозиторий на GitHub
1. Откройте https://github.com
2. Нажмите зеленую кнопку "New" (Новый репозиторий)
3. Введите название: `LoaderApp` 
4. **ВАЖНО**: НЕ добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### Шаг 2: Загрузите код
Распакуйте ZIP архив и выполните в терминале:

```bash
cd LoaderApp
git init
git add .
git commit -m "Первый коммит"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/LoaderApp.git
git push -u origin main
```

**Замените `ВАШ_ЛОГИН` на ваше имя пользователя GitHub!**

### Шаг 3: Получите готовый APK
1. GitHub автоматически начнет сборку (3-5 минут)
2. Перейдите в ваш репозиторий на GitHub
3. Нажмите вкладку **Actions**
4. Дождитесь зеленой галочки ✅
5. Нажмите на последний успешный запуск
6. В разделе **Artifacts** скачайте **app-debug-apk**
7. Внутри будет файл `app-debug.apk`

### Шаг 4: Установите APK на телефон
1. Скопируйте `app-debug.apk` на телефон
2. В настройках Android включите "Установка из неизвестных источников"
3. Откройте APK файл
4. Нажмите "Установить"
5. Готово! 🎉

---

## Вариант 2: Ручная сборка на компьютере

### Требования:
- Android Studio
- JDK 17
- Android SDK

### Шаги:
1. Распакуйте ZIP архив
2. Откройте Android Studio
3. File → Open → выберите папку `LoaderApp`
4. Дождитесь синхронизации Gradle (несколько минут)
5. Build → Build Bundle(s) / APK(s) → Build APK(s)
6. APK будет в `app/build/outputs/apk/debug/app-debug.apk`

### Или через командную строку:
```bash
cd LoaderApp
./gradlew assembleDebug
```

APK будет тут: `app/build/outputs/apk/debug/app-debug.apk`

---

## 🔧 Если что-то пошло не так

### Ошибка "SDK not found"
Создайте файл `local.properties` в корне проекта:
```
sdk.dir=C:\\Users\\ВашеИмя\\AppData\\Local\\Android\\Sdk
```
(Укажите путь к вашему Android SDK)

### Ошибка "Permission denied: ./gradlew"
```bash
chmod +x gradlew
```

### Ошибка "JAVA_HOME not set"
Установите JDK 17 и настройте переменную окружения

### GitHub Actions не работает
- Убедитесь что `.github/workflows/android.yml` загружен
- Проверьте что все файлы загрузились (особенно `gradlew`)

---

## 📝 Структура файлов после распаковки

```
LoaderApp/
├── .github/
│   └── workflows/
│       └── android.yml      ← Автоматическая сборка
├── app/
│   ├── build.gradle         ← Конфигурация приложения
│   └── src/
│       └── main/
│           ├── AndroidManifest.xml
│           ├── java/         ← Код на Kotlin
│           └── res/          ← Ресурсы
├── gradle/
│   └── wrapper/             ← Gradle wrapper
├── gradlew                  ← Скрипт сборки (Linux/Mac)
├── gradlew.bat              ← Скрипт сборки (Windows)
├── build.gradle             ← Главный конфиг Gradle
├── settings.gradle
└── README.md
```

---

## ✅ Проверка перед загрузкой

Запустите скрипт проверки:
```bash
cd LoaderApp
./check-project.sh
```

Должны быть ✅ на всех пунктах.

---

## 💡 Полезные команды

Очистить кеш и пересобрать:
```bash
./gradlew clean
./gradlew assembleDebug
```

Посмотреть подробные логи при ошибке:
```bash
./gradlew assembleDebug --stacktrace
```

Собрать релизную версию (требует подпись):
```bash
./gradlew assembleRelease
```

---

## 📞 Что дальше?

После успешной установки:
1. При первом запуске создайте профиль
2. Выберите роль (Диспетчер или Грузчик)
3. Если Диспетчер - создавайте заказы кнопкой "+"
4. Если Грузчик - берите заказы во вкладке "Доступные"

Приложение готово к использованию! 🚀
