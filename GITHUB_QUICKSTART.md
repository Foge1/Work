# 🚀 Быстрый старт для GitHub

## Загрузка в GitHub

1. **Создайте новый репозиторий на GitHub** (без README, .gitignore и лицензии)

2. **Загрузите проект:**
```bash
cd LoaderApp
git init
git add .
git commit -m "Initial commit: LoaderApp"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

## Автоматическая сборка

После пуша в GitHub:
1. Перейдите в **Actions** в вашем репозитории
2. GitHub автоматически начнет сборку
3. Через 3-5 минут APK будет готов
4. Скачайте его в разделе **Artifacts**

## Ручная сборка локально

### Linux/Mac:
```bash
./gradlew assembleDebug
```

### Windows:
```bash
gradlew.bat assembleDebug
```

APK будет здесь: `app/build/outputs/apk/debug/app-debug.apk`

## Быстрая проверка проекта

```bash
./check-project.sh
```

## Что делать если не собирается

1. **Проверьте Java:**
```bash
java -version
```
Должна быть версия 17+

2. **Очистите кеш Gradle:**
```bash
./gradlew clean
```

3. **Пересоберите:**
```bash
./gradlew assembleDebug
```

4. **Проверьте логи:**
```bash
./gradlew assembleDebug --stacktrace
```

## Установка APK на телефон

1. Включите "Установка из неизвестных источников" в настройках
2. Скопируйте APK на телефон
3. Откройте APK файл
4. Разрешите установку
5. Готово!

## Возможные проблемы

### "SDK not found"
Установите Android SDK через Android Studio или создайте `local.properties`:
```
sdk.dir=/path/to/android/sdk
```

### "Permission denied: ./gradlew"
```bash
chmod +x gradlew
```

### "JAVA_HOME not set"
```bash
export JAVA_HOME=/path/to/jdk17
```

## Поддержка

Если возникли проблемы, создайте Issue в репозитории с описанием ошибки и логами.
